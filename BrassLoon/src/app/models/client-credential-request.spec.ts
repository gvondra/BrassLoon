import { ClientCredentialRequest } from './client-credential-request';

describe('ClientCredentialRequest', () => {
  it('should create an instance', () => {
    expect(new ClientCredentialRequest()).toBeTruthy();
  });
});
